interface:
  display_name: "Phase Gate Orchestrator"
  short_description: "Plan, review, and commit work with headless executors"
  default_prompt: "Use $phase-gate-orchestrator to create and complete a phased plan in the target repository, launching headless executors while you review their code, request repairs, commit approved work, and continue through every phase."
