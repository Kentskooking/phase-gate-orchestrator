interface:
  display_name: "Phase Gate Orchestrator"
  short_description: "Gate executor work with review and resume checks"
  default_prompt: "Use $phase-gate-orchestrator to run a bounded implementation phase against an explicit target repository with packaged scripts, semantic review, and a targeted commit gate."
